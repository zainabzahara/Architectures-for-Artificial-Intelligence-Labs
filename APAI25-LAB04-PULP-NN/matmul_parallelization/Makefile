# APPLICATION NAME
APP = matrixMul

# SOURCE FILES
APP_SRCS = main.c cluster.c matmuls.c

# NUMBER OF USED CLUSTER CORES
CORES ?= 1

# PERFORMANCE COUNTERS
APP_CFLAGS += -DPERF

# CHECK RESULT
APP_CFLAGS += -DDEBUG

# COMPILER FLAGS
APP_CFLAGS += -O3 -g3 -DNUM_CORES=${CORES} -DSTACK_SIZE=1024
APP_LDFLAGS +=

APP_CFLAGS += -DSTATS


include $(RULES_DIR)/pmsis_rules.mk
